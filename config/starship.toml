# Inserts a blank line between shell prompts
add_newline = true
# double the command_timeout (default 500ms)
command_timeout = 1000

[directory]
truncate_to_repo = false
truncation_length = 2
truncation_symbol = "…/"

[git_branch]
symbol = ""

[time]
disabled = false
